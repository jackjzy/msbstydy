<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.combomeal.CombomealCategoryMapper">

    <resultMap type="FfwyCombomeal" id="FfwyCombomealResult">
        <result property="parentId" column="parent_id"/>
        <result property="combomealName" column="combomaeal_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="categoryId" column="category_id"/>
    </resultMap>

    <sql id="CombomealCategoryVo">
        select category_id , parent_id, combomaeal_name, create_time, update_time from ffwy_combomeal_category
    </sql>

    <select id="selectCombomealCategoryList" parameterType="Long" resultMap="FfwyCombomealResult">
        <include refid="CombomealCategoryVo"/>
        <where>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>

</mapper>